import { PlaceholderPage } from "@/components/app-shell/placeholder-page";

export default function AircraftPage() {
  return (
    <PlaceholderPage
      description="Fleet status, aircraft readiness, and aircraft-specific operational context will be added in a later slice."
      eyebrow="Fleet"
      title="Aircraft"
    />
  );
}
